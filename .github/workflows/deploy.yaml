---
name: Deploy

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main
  release:
    types:
      - published
  workflow_dispatch:

permissions: {}

jobs:
  ci:
    permissions:
      actions: read
      contents: read
      packages: read
      security-events: write
    uses: casse-boubou/ha-addon-workflows/.github/workflows/app-ci.yaml@079177fd2a0c3e913c7c0b343c124e4f5ff3f82b # main

  deploy:
    needs: ci
    permissions:
      contents: read
      packages: write
    uses: casse-boubou/ha-addon-workflows/.github/workflows/app-deploy.yaml@079177fd2a0c3e913c7c0b343c124e4f5ff3f82b # main
    secrets:
      DISPATCH_TOKEN: ${{ secrets.DISPATCH_TOKEN }}
